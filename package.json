{
    "name": "search",
    "homepage": "https://evbogdanov.github.io/search",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "prop-types": "^15.8.0",
        "react": "^17.0.2",
        "react-dom": "^17.0.2",
        "react-scripts": "5.0.0"
    },
    "scripts": {
        "start": "react-scripts start",
        "build": "react-scripts build",
        "test": "react-scripts test --env=jsdom",
        "eject": "react-scripts eject",
        "publish": "gh-pages -d build",
        "clean-build-dir": "rm -r build",
        "deploy": "npm-run-all build publish clean-build-dir",
        "prettier-check": "prettier --check ./src",
        "prettier-fix": "prettier --write ./src"
    },
    "devDependencies": {
        "gh-pages": "^3.2.3",
        "npm-run-all": "^4.1.5",
        "prettier": "2.5.1"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    }
}
